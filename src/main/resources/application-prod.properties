# Production Environment Overrides (Values injected via Render Environment Variables)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres}
spring.datasource.username=${DB_USERNAME:postgres.vdckkiciupwulpwmcaop}
spring.datasource.password=${DB_PASSWORD:!,k9Z$XYr*Cm3jf}

# STRICT Database security: Do not alter tables automatically, do not show SQL
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Prod Email Config
spring.mail.username=${MAIL_USERNAME:evaira.shopping@gmail.com}
spring.mail.password=${MAIL_PASSWORD:fzkyakyvkwjurgml}

# Prod JWT Config (Use a stronger secret in prod)
jwt.secret=${JWT_SECRET:change_this_super_secret_key_123}
jwt.expiration-ms=${JWT_EXPIRATION_MS:864000000}

# HikariCP (VERY IMPORTANT)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=60000
spring.datasource.hikari.keepalive-time=30000
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.connection-test-query=SELECT 1

# Disable Swagger in Production
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true

# Supabase Auth configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=${SUPABASE_ISSUER_URI:https://vdckkiciupwulpwmcaop.supabase.co/auth/v1}

# # Spring Security Debug Logs (Shows exact reason for 401 errors)
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2=DEBUG
